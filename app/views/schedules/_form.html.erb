<%= form_for(schedule) do |f| %>
  <% if schedule.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(schedule.errors.count, "error") %> prohibited this schedule from being saved:</h2>

      <ul>
      <% schedule.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :start %>
    <%= f.text_field :start %>
  </div>

  <div class="field">
    <%= f.label :end %>
    <%= f.text_field :end %>
  </div>

  <div class="field">
    <%= f.label :non_dtop %>
    <%= f.text_field :non_dtop %>
  </div>

  <div class="field">
    <%= f.label :t_arrive %>
    <%= f.time_select :t_arrive %>
  </div>

  <div class="field">
    <%= f.label :day %>
    <%= f.text_area :day %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
